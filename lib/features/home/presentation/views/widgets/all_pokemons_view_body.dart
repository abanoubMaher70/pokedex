import 'package:flutter/material.dart';

class AllPokemonsViewBody extends StatelessWidget {
  const AllPokemonsViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [Text("data")]);
  }
}
